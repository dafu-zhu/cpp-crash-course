import { C, P, H, B, Code, AnnotatedCode, Step, Flowchart, MemDiagram, Hierarchy, Prof, Exam, Tip, Confusion, Conversion, Hw, Quiz, Checklist } from "../components";

const module2 = {id:2,title:"OOP & Classes",sub:"Lec 2 · Classes, Templates, STL Containers, Control Flow",icon:"🏗️",color:C.b,sections:[{title:"Why We Need Classes",content:(<><P>The professor opens Lecture 2 with a concrete goal: we build a <B>Portfolio Manager</B>. But first:</P><Flowchart title="What do we need?" steps={[{label:"Step 1",items:["Represent a Stock"],color:C.r},{label:"Step 2",items:["Store multiple Stocks"],color:C.b},{label:"Step 3",items:["Do computations"],color:C.o},{label:"Step 4",items:["Handle errors"],color:C.g}]}/><Prof>"Can we represent a stock using a fundamental type? We cannot. A stock has many features — a symbol, a price, a quantity. We don't have one type that can capture all the information about a stock."</Prof><P>An int stores quantity, a double stores price, a string stores symbol — but no single type stores all three AND provides operations (read price, change price). That's what <B>classes</B> solve: bundle data + operations into one custom type.</P><Prof>"Storing data is not enough. We have to do various operations on them. We may want to read the price. Stock prices change. We may want to change the price."</Prof><Step n={1} title="A class bundles DATA (what it HAS) and OPERATIONS (what you DO with it)">Data members (attributes): symbol_, price_, qty_. Member functions (operations): get_symbol(), get_price(), set_price(). Together, they form a single unit called Stock.</Step><Quiz questions={[{q:"Why can't a single fundamental type represent a Stock?",o:["Stocks are complex financial instruments","A Stock needs multiple data fields AND operations on them","Fundamental types are too slow","C++ doesn't have a stock type"],a:1},{q:"Operations on a class are called:",o:["Data members","Attributes","Member functions","Variables"],a:2}]}/></>)},

{title:"Building the Stock Class Step by Step",content:(<><Flowchart title="Strategy: Building a Stock Class" steps={[{label:"1. Data",items:["symbol (string)","price (double)","qty (int)"],color:C.b},{label:"2. Operations",items:["get/set functions"],color:C.g},{label:"3. Protection",items:["public: functions","private: data"],color:C.o},{label:"4. Constructors",items:["Default: Stock()","Stock(sym,price,qty)"],color:C.accent}]}/><H>First: Choose Data and Operations</H><P>The professor asked the class what attributes a stock needs. Answers: symbol/ticker, price, position size (quantity). For operations: read symbol, read price, change price, change quantity. Notice: <B>no set_symbol()</B> — the professor made a deliberate design choice.</P><Prof>"For a stock, changing the symbol may not make much sense. We can decide what we want to change and what we don't want to change. That's the idea."</Prof><H>The Raw Class Syntax</H><AnnotatedCode title="class Stock — first attempt" lines={[{code:"class Stock {",why:"'class' keyword starts a new type definition. 'Stock' is the name. This defines a new type AND a new scope."},{code:"    string symbol_;",why:"Data member: stores the ticker. Trailing underscore _ = course naming convention for private members."},{code:"    double price_;",why:"Data member: stock price. double because finance needs 15 digits of precision."},{code:"    int qty_;",why:"Data member: number of shares. int because no fractional shares."},{code:"    string get_symbol();",why:"Member function: returns the symbol. This is an 'operation' on the data."},{code:"    double get_price();",why:"Member function: returns the price. 'Getter' — lets us READ private data."},{code:"    void set_price(double price);",why:"Member function: changes the price. void = returns nothing. 'Setter' — lets us CHANGE private data."},{code:"};",why:"CRITICAL: semicolon after closing brace! The professor warned: 'C++ compiler will give you a lot of grief if you forget.'"}]}/><Confusion mistake="Forgetting the semicolon after the closing brace of a class" why="Unlike functions, class/struct definitions MUST end with };. Without it, the compiler tries to treat everything after as part of the class, causing confusing cascading errors."/><H>Add the Coding Standard</H><Tip title="FINM 326 Coding Standard (points deducted if not followed!)">1. Each class in a <B>separate .h file</B> (definition) and <B>.cpp file</B> (implementation){"\n"}2. Class names: <B>uppercase first letter</B> — Stock, PortfolioMgr{"\n"}3. Member functions: <B>snake_case</B> — get_price(), set_qty(){"\n"}4. Private members: <B>trailing underscore</B> — symbol_, price_{"\n"}5. For anything else: refer to Google C++ Style Guide</Tip><Prof>"In the industry, if you write something, other people are going to read it, use it, and change it. If everyone writes however they want, that creates headaches. So we use coding standards."</Prof><H>Add Protection Levels</H><Step n={2} title="Keep data private, expose operations as public">This is a core OOP principle. The professor calls it: "Keep the data members internal to the class; allow outside access using member functions."</Step><AnnotatedCode title="Stock with protection levels" lines={[{code:"class Stock {",why:"Begin class definition."},{code:"public:",why:"Everything below this is accessible to anyone. This is the 'interface.'"},{code:"    string get_symbol();",why:"Public: anyone can read the symbol."},{code:"    double get_price();",why:"Public: anyone can read the price."},{code:"    void set_price(double price);",why:"Public: anyone can change the price."},{code:"private:",why:"Everything below is hidden. Only Stock's own functions can access these."},{code:"    string symbol_;",why:"Hidden. Must go through get_symbol() to read."},{code:"    double price_;",why:"Hidden. Must use get_price()/set_price()."},{code:"    int qty_;",why:"Hidden."},{code:"};",why:"End of class."}]}/><Confusion mistake="Trying s1.price_ = 500; from outside the class" why="price_ is private. Compiler error: 'cannot access private member.' You MUST use the public setter: s1.set_price(500); This is the whole point of protection levels — controlling access."/><H>Add Constructors and Destructor</H><P>A <B>constructor</B> initializes data when an object is created. Same name as the class, returns nothing. A <B>destructor</B> (~Stock) cleans up when the object is destroyed.</P><AnnotatedCode title="Complete definition — stock.h" lines={[{code:"#pragma once",why:"Include guard — prevents this header from being read twice."},{code:'#include <string>',why:"Need the string type."},{code:"using std::string;",why:"So we write 'string' not 'std::string' everywhere."},{code:"",why:""},{code:"class Stock {",why:""},{code:"public:",why:""},{code:"    Stock();",why:"DEFAULT constructor: no arguments. Creates empty stock."},{code:"    Stock(string symbol, double price, int qty);",why:"PARAMETERIZED constructor: the useful one. A stock without symbol/price is meaningless."},{code:"    ~Stock();",why:"Destructor: called when object destroyed. Does nothing here but will be important later for resource cleanup."},{code:"    string get_symbol();",why:"Getter."},{code:"    double get_price();",why:"Getter."},{code:"    int get_qty();",why:"Getter."},{code:"    void set_price(double price);",why:"Setter."},{code:"    void set_qty(int qty);",why:"Setter."},{code:"private:",why:""},{code:"    string symbol_;",why:""},{code:"    double price_;",why:""},{code:"    int qty_;",why:""},{code:"};",why:""}]}/><H>Implement the Member Functions</H><P>Definition (.h) says WHAT exists. Implementation (.cpp) says HOW it works. Each function is prefixed with <code style={{background:"rgba(255,255,255,0.08)",padding:"2px 6px",borderRadius:4}}>Stock::</code> — the scope resolution operator — meaning "this function belongs to Stock."</P><Conversion from="Assignment in constructor body (two operations per member)" to="Member initializer list (one operation per member)" feature="Member Initializer List (the colon : syntax)" beforeCode={`Stock::Stock(string s,
    double p, int q) {
  symbol_ = s;  // default-construct
  price_ = p;   // THEN assign
  qty_ = q;     // two operations!
}`} afterCode={`Stock::Stock(string s,
    double p, int q)
  : symbol_(s),  // initialize
    price_(p),   // directly when
    qty_(q)      // created: ONE op!
{ }`}/><Prof>"The proper way to initialize members in a constructor is to use member initializer lists. This is an optimization every C++ programmer should know."</Prof><AnnotatedCode title="stock.cpp — implementation" lines={[{code:'#include "stock.h"',why:"Include our header so the .cpp can see the class definition."},{code:"",why:""},{code:"Stock::Stock(string symbol, double price, int qty)",why:"Parameterized constructor. Stock:: means 'belongs to Stock class.'"},{code:"    : symbol_(symbol), price_(price), qty_(qty)",why:"MEMBER INITIALIZER LIST: each member initialized directly. More efficient than assignment in body."},{code:"{ }",why:"Body can be empty — all init done in the list."},{code:"",why:""},{code:"Stock::Stock()",why:"Default constructor."},{code:'    : symbol_(""), price_(0.0), qty_(0)',why:"Initialize to empty/zero defaults."},{code:"{ }",why:""},{code:"",why:""},{code:"Stock::~Stock() { }",why:"Destructor: nothing to clean up yet."},{code:"",why:""},{code:"double Stock::get_price() {",why:"get_price belongs to Stock (Stock::)."},{code:"    return price_;",why:"Returns a COPY of the price value."},{code:"}",why:""},{code:"",why:""},{code:"void Stock::set_price(double price) {",why:"set_price belongs to Stock. Takes new price as param."},{code:"    price_ = price;",why:"Overwrites private member. This is the ONLY way to change price from outside."},  {code:"}",why:""}]}/><H>Use the Stock Class</H><AnnotatedCode title="main.cpp" lines={[{code:'#include "stock.h"',why:"Include Stock so main knows about it."},{code:"#include <iostream>",why:"For cout."},{code:"using std::cout; using std::endl;",why:"Qualify individually (professor's preferred technique)."},{code:"",why:""},{code:"int main() {",why:""},{code:'    Stock s1("MSFT", 500.0, 10);',why:"Create object s1 — calls parameterized constructor. s1 IS an instance of Stock."},{code:'    Stock s2("GOOG", 250.0, 5);',why:"Another object. Same class, different data. Each has its own copy of data members."},{code:"",why:""},{code:"    cout << s1.get_symbol() << endl;",why:"DOT NOTATION: call get_symbol() ON s1. Prints 'MSFT'."},{code:"    s1.set_price(520.0);",why:"Change s1's price through the public setter."},{code:'    // s1.symbol_ = "AAPL";',why:"ERROR! symbol_ is private. Cannot access directly."},  {code:"}",why:""}]}/><Tip title="Class vs Object">CLASS = the blueprint/code you write (Stock). Defines a new type.{"\n"}OBJECT = a specific instance (s1, s2). Each has its own data.{"\n\n"}Analogy: class = cookie cutter (shape). Object = individual cookie.</Tip><Quiz questions={[{q:"Why use member initializer lists?",o:["Shorter code","Members initialized directly (one op vs two)","Required by compiler","No difference"],a:1},{q:"What happens with `Stock s; s.price_ = 100;`?",o:["Sets price to 100","Compile error — price_ is private","Runtime error","Nothing"],a:1,e:"Private members cannot be accessed outside the class."},{q:"The trailing underscore in symbol_ means:",o:["It's a pointer","It's a constant","Private member (naming convention)","It's static"],a:2}]}/></>)},

{title:"Encapsulation & Data Abstraction",content:(<><H>Encapsulation</H><Step n={1} title="Encapsulation = data + functions bundled, access controlled"><P>Data (symbol_, price_) and functions (get_price, set_price) combined into one unit. Data is private — accessed only through public functions.</P></Step><Prof>"Encapsulation refers to combining data and functions inside a class so that data is only accessed through the class functions."</Prof><H>Data Abstraction</H><Step n={2} title="Data Abstraction = interface separated from implementation"><P>The INTERFACE (public functions — what you CAN do) is separate from the IMPLEMENTATION (how functions work internally). Users don't need to know internals.</P></Step><Prof>"If I'm going to use the Stock class, I need to know how to create a Stock and what functions I have. I don't need to know how get_price() is implemented. You can change the implementation to Black-Scholes today, Monte Carlo tomorrow — users just call price()."</Prof><MemDiagram title="Interface vs Implementation" cells={[{label:"INTERFACE",value:"What you see",color:C.g,addr:"public functions"},{type:"arrow",label:"wall"},{label:"IMPLEMENTATION",value:"How it works",color:C.r,addr:"hidden details"}]}/><Exam>Slide 31 homework: "What are the benefits of Encapsulation and Data Abstraction?" Answer: (1) Reuse — classes as building blocks. (2) Maintainability — change implementation without affecting users. (3) Protection — private data safe from corruption. (4) Clear interfaces.</Exam></>)},

{title:"struct vs class & Include Guards",content:(<><H>struct vs class</H><Step n={1} title="Only difference: default protection level">In a class, members default to private. In a struct, members default to public. That is the ONLY difference. Structs can have functions, constructors, inheritance — everything.</Step><Exam>Slide 39 interview question: "What are the differences between a struct and a class?" Answer: Only the default protection level.</Exam><H>Include Guards</H><Prof>"If you include this header twice, you're defining the stock class twice. That's an error — redefining something doesn't make sense. This is the One Definition Rule."</Prof><AnnotatedCode title="Standard include guard" lines={[{code:"#ifndef STOCK_H",why:"IF NOT DEFINED: first time, STOCK_H isn't defined, so we enter this block."},{code:"#define STOCK_H",why:"DEFINE it now. Next time preprocessor sees #ifndef, STOCK_H IS defined → skip."},{code:"class Stock { /* ... */ };",why:"Class definition only read once."},{code:"#endif",why:"End of conditional block."}]}/><Code title="Non-standard shortcut (Visual Studio only)" code={`#pragma once  // same effect, one line, but not portable`}/><Exam>Both struct/class difference AND include guards are listed as interview questions on Slide 39.</Exam><Quiz questions={[{q:"struct vs class?",o:["Structs can't have functions","Default access: class=private, struct=public","Structs are faster"],a:1},{q:"Standard include guard technique?",o:["#pragma once","#ifndef/#define/#endif"],a:1}]}/></>)},

{title:"Templates (Generic Programming)",content:(<><P>The professor introduces templates so we can understand STL containers (which use templates).</P><Conversion from="Duplicate functions for each type" to="One function for all types" feature="Function Template (template &lt;typename T&gt;)" beforeCode={`int add(int a, int b)
  { return a+b; }
double add(double a, double b)
  { return a+b; }
// Same logic, different types!`} afterCode={`template <typename T>
T add(const T a, const T b)
  { return a+b; }
// ONE function, ALL types!
// Compiler generates versions
// as needed`}/><Prof>"T is not a type. T is a placeholder for a type. When you use add(1,2), the compiler sees ints and creates an int version."</Prof><AnnotatedCode title="Using templates" lines={[{code:"int r1 = add(1, 2);",why:"Compiler deduces T=int → generates add<int> → returns 3."},{code:"double r2 = add(1.5, 2.5);",why:"Compiler deduces T=double → generates add<double> → returns 4.0."},{code:"// add(1, 2.5);",why:"ERROR! 1 is int, 2.5 is double → compiler can't decide T."},{code:"double r3 = add<double>(1, 2.5);",why:"FIX: explicitly specify T=double. Int 1 promoted to 1.0."}]}/><Confusion mistake="Calling template with mismatched types: add(1, 2.5)" why="T can only be one type. 1 is int, 2.5 is double — ambiguous. Specify explicitly: add<double>(1, 2.5)."/><H>Class Templates</H><AnnotatedCode lines={[{code:"template <typename T>",why:"T is a placeholder type."},{code:"class SimpleVector {",why:"A class that stores elements of type T."},{code:"    T values[10];",why:"If T=int → int array. If T=Stock → Stock array."},{code:"};",why:""},{code:"SimpleVector<int> v1;",why:"Compiler generates class with int values[10]."},{code:"SimpleVector<Stock> v3;",why:"Works with custom types too!"}]}/><P>This is how vector works: <code style={{background:"rgba(255,255,255,0.08)",padding:"2px 6px",borderRadius:4}}>vector&lt;int&gt;</code> is the vector class template with T=int.</P><Tip title="Template Source Organization (Slide 54)">1. <B>Inclusion technique</B>: put template implementation in the header (most common){"\n"}2. <B>Explicit instantiation</B>: instantiate for specific types in .cpp{"\n"}For now: write templates in the same file as main when experimenting.</Tip><Quiz questions={[{q:"template <typename T> means:",o:["T is an integer","T is a placeholder for any type","T is a template","T must be double"],a:1},{q:"std::vector is a:",o:["Function","Class template","Fundamental type"],a:1}]}/></>)},

{title:"STL: vector, map, pair",content:(<><Prof>"By default, use vector when you need a container." — Bjarne Stroustrup</Prof><H>std::vector</H><AnnotatedCode lines={[{code:"#include <vector>",why:"Vector header."},{code:"using std::vector;",why:"So we write vector<int> not std::vector<int>."},{code:"",why:""},{code:"vector<int> v;",why:"Empty vector. Size=0."},{code:"v.push_back(10);",why:"Add 10 to end. push_back creates space if needed (can be expensive)."},{code:"v.push_back(20);",why:"Vector now {10, 20}."},{code:"",why:""},{code:"vector<int> v2(10);",why:"Vector with 10 elements, all initialized to 0."},{code:"v2[0] = 42;",why:"Access by index, like an array."},{code:"",why:""},{code:"vector<Stock> portfolio;",why:"Template magic: stores Stock objects!"},{code:'portfolio.push_back(Stock("MSFT", 500.0, 10));',why:"Add Stock to portfolio."}]}/><H>std::map</H><Prof>"A vector lets you access by integer index. But what if you want to access by stock symbol? That's a map."</Prof><AnnotatedCode lines={[{code:"#include <map>",why:""},{code:"std::map<string, Stock> stocks;",why:"Key=string (symbol), Value=Stock. Look up stocks by name."},{code:'stocks.insert({"AAPL", Stock("AAPL", 150.0, 2)});',why:"Insert key-value pair."},{code:'stocks.insert({"MSFT", Stock("MSFT", 500.0, 1)});',why:"Another pair. Keys sorted, must be unique."}]}/><H>std::pair</H><AnnotatedCode lines={[{code:'pair<string, Stock> p("MSFT", Stock("MSFT", 500.0, 5));',why:"Stores exactly two values. first=key, second=value. Each map element IS a pair."},{code:"stocks.insert(p);",why:"Insert pair into map."},{code:'stocks.insert(make_pair("GOOG", Stock("GOOG", 200.0, 1)));',why:"make_pair convenience function."}]}/><Quiz questions={[{q:"push_back() does what?",o:["Removes last","Adds to end, growing if needed","Inserts at start","Sorts"],a:1},{q:"Each map element is a:",o:["value only","key only","std::pair (key+value)","iterator"],a:2}]}/></>)},

{title:"Control Flow: if/else, switch, enum, Loops",content:(<><P>The professor: "Any useful program branches or loops. You cannot write meaningful programs without them."</P><H>if/else</H><Code code={`if (score >= 95) grade = "A";
else if (score >= 90) grade = "A-";
else if (score >= 85) grade = "B+";
else grade = "F";`}/><H>switch (expression must be integer or enum)</H><AnnotatedCode lines={[{code:"switch(value) {",why:"Jump to matching case."},{code:'case 0: cout << "zero"; break;',why:"break EXITS the switch. Without it, falls through to next case!"},{code:'case 1: cout << "one"; break;',why:""},{code:'default: cout << "other";',why:"If no case matches."}]}/><Confusion mistake="Forgetting break in switch" why="Without break, execution falls through. value=0 without break prints 'zero' AND 'one' AND 'other'. Professor assigned this as homework."/><H>enum — Named Integer Constants</H><Code code={`enum class TradingSignal { HOLD=0, BUY=1, SELL=2 };
TradingSignal sig = TradingSignal::BUY;`}/><H>for loop</H><AnnotatedCode lines={[{code:"for (int n = 0; n < 10; ++n) {",why:"(1) init n=0. (2) condition: continue while n<10. (3) increment n after each iteration."},{code:'    cout << n << endl;',why:"Runs 10 times: n = 0,1,2,...,9."},{code:"}",why:"After n=10, condition fails, loop exits."}]}/><H>while loop</H><AnnotatedCode lines={[{code:"int n = 0;",why:"Init before while."},{code:"while (n < 10) {",why:"Check condition FIRST."},{code:"    cout << n << endl;",why:""},{code:"    n++;",why:"MUST alter condition! Otherwise infinite loop."}]}/><Prof>"You have to have some statement that alters the condition. Otherwise, you are going to be running inside this loop forever."</Prof><H>do/while</H><Code code={`do {
    cout << "Enter number (0 to end): ";
    cin >> n;
} while (n != 0);  // body runs AT LEAST once`}/><Tip title="Homework Problems (Slides 87-89)">1. <B>Factorial:</B> n! = n × (n-1) × ... × 1{"\n"}2. <B>Prime:</B> no divisors except 1 and itself (use % operator){"\n"}3. <B>Fibonacci:</B> 0, 1, 1, 2, 3, 5, 8, 13...{"\n"}4. <B>Babylonian sqrt:</B> guess = (guess + n/guess)/2, repeat{"\n"}5. <B>Bubble Sort:</B> swap adjacent if out of order, repeat</Tip><Quiz questions={[{q:"Missing 'break' in switch causes:",o:["Compile error","Falls through to next case","Normal exit","Runtime error"],a:1},{q:"while loop with no condition-altering statement:",o:["Runs once","Correct iterations","Infinite loop","Compile error"],a:2}]}/></>)},

{title:"Iterators & auto Keyword",content:(<><P>We have data in containers. Now we traverse them using <B>iterators</B> — generalized pointers representing positions in a container.</P><H>Evolution of Syntax (old → modern)</H><Step n={1} title="Old: explicit iterator type">
<Code code={`for (vector<int>::iterator iter = v.begin();
     iter != v.end(); ++iter)
    cout << *iter << endl;  // * dereferences`}/>
</Step><Step n={2} title="Better: auto deduces the type">
<Code code={`for (auto iter = v.begin(); iter != v.end(); ++iter)
    cout << *iter << endl;`}/>
</Step><Step n={3} title="Objects: use arrow -> for member access">
<Code code={`for (auto it = portfolio.begin(); it != portfolio.end(); ++it)
    cout << it->get_symbol() << ": " << it->get_price();`}/>
</Step><Step n={4} title="Maps: each element is a pair (.first/.second)">
<Code code={`for (auto it = stocks.begin(); it != stocks.end(); ++it)
    cout << it->first << ": " << it->second.get_price();`}/>
</Step><Step n={5} title="Best: range-based for (modern C++, preferred!)">
<Code code={`for (const auto& stock : portfolio)
    cout << stock.get_symbol() << endl;
// Clean, readable, no iterators needed`}/>
</Step><Confusion mistake="Using dot (.) with iterators: iter.get_price()" why="Iterators point TO elements like pointers. Use arrow (->): iter->get_price(). Dot is for objects directly."/><H>auto Keyword (C++11)</H><AnnotatedCode lines={[{code:"auto x = 1;",why:"Compiler sees 1 → x is int."},{code:"auto y = 1.0;",why:"1.0 → double."},{code:"auto& rx = x;",why:"Reference to int."},{code:"const auto z = 3;",why:"const int."}]}/><Hw num={1} title="Simple Portfolio Manager (3%)" desc={`Write Stock class in stock.h/stock.cpp. Store 5 stocks in vector<Stock>.
Compute portfolio value: V = Σ(price_i × qty_i). Print to console.`} practice={`StudentGradeBook: Student class (name, id, vector<double> scores).
5 students, 3+ scores each. Store in vector<Student>. Compute averages.
Find highest average. Use coding standard.`}/><Checklist items={["Write complete Stock class with coding standard","Use member initializer lists","Understand public vs private","Explain encapsulation & abstraction","struct vs class (default access only)","Include guards: #ifndef AND #pragma once","Function templates & class templates","vector: push_back, index, size","map: key-value pairs, insert, lookup","for, while, do/while loops + switch/enum","Iterate with iterators AND range-based for","Use auto for type deduction","Write: factorial, prime, fibonacci"]}/></>)}
]};

export default module2;
